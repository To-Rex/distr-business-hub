[build]
  command = "npm run build"
  publish = "dist/client"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."ssr-serve"]
  included_files = ["dist/server/**"]

[build.environment]
  NODE_VERSION = "20"
